def config = new HashMap < String, HashMap < String, String > >()

config.organization = new HashMap()
config.organization.title = 'Foxcapades'
config.organization.slug  = 'io.vulpine'
config.organization.key   = 'foxcapades'
config.organization.site  = "https://github.com/${config.organization.key}"

config.project = new HashMap()
config.project.group       = 'lib'
config.project.name        = 'sql-import'
config.project.version     = '0.2.1'
config.project.slug        = 'sql-import'
config.project.jvm         = '1.10'
config.project.description = 'SQL resource loading'
config.project.site        = "${config.organization.site}/java-${config.project.slug}"
config.project.vcs         = "${config.organization.site}/java-${config.project.slug}.git"
config.project.tracker     = "${config.organization.site}/java-${config.project.slug}/issues"

config.developer = new HashMap()
config.developer.name  = 'Elizabeth Harper'
config.developer.email = 'elliefops@gmail.com'
config.developer.user  = 'elliefops'
config.developer.site  = 'https://github.com/Foxcapades'

config.license = new HashMap()
config.license.name = 'Apache License, Version 2.0'
config.license.url  = 'http://www.apache.org/licenses/LICENSE-2.0.txt'

ext.config = config
